# web/ — React + Vite + TypeScript frontend

Scaffolded in Phase 7 (`npm create vite`). Placeholder directories (`src/lib`, `src/components`, `src/pages`) mark the intended layout from the build plan §5.
