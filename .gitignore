# --- Secrets: NEVER commit real keys ---
.env
.env.*
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.cache/
.mypy_cache/
dist/
build/

# --- Node / frontend ---
node_modules/
web/dist/
web/.vite/
*.local

# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# --- Claude Code local config ---
.claude/

# --- Internal working docs (kept locally for continuity, not published) ---
docs/HANDOFF.md
docs/2026-06-07-india-aqi-weather-dashboard-build-plan.md
docs/SETUP.md

# --- Generated data (regenerated by the pipeline and published to the `data` branch by CI;
#     not committed during dev to avoid churn + stale partial samples). Manual meta
#     source files (station_city_overrides.json, city_aliases.json) are NOT ignored. ---
data/history/*.parquet
data/recent/*.parquet
data/live/*.json
data/meta/city_list.json
data/meta/cities.json
data/meta/coverage.json
data/meta/station_city_map.json
data/meta/unmapped_stations.json

# Frontend dev data symlink (-> repo data/, regenerated by pipeline)
web/public/data
