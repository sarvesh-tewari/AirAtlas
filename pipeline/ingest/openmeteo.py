"""Open-Meteo fetch: current weather (Forecast API) + historical (Archive/ERA5 API).

Phase 3. No API key required. Pulls temperature, humidity, rainfall, wind for each
target city — current (hourly) and multi-year archive (daily delta).
"""
